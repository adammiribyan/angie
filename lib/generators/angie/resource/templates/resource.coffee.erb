angular.module('blueprint').factory <%= "'#{resource_name}'" %>, (<%= model_name %>, ResourceBase) ->
  class <%= resource_name %> extends ResourceBase
    constructor: ->
      super <%= "'/#{path_name}/:id'" %>, default: { model: <%= model_name %> }
